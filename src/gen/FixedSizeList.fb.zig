//!
//! generated by flatc-zig
//! binary:     src/gen/format/Schema.bfbs
//! schema:     format/Schema.fbs
//! file ident: //Schema.fbs
//! typename    FixedSizeList
//!

const std = @import("std");
const fb = @import("flatbufferz");
const Builder = fb.Builder;

pub const FixedSizeListT = struct {
    /// Number of list items per value
    listSize: i32 = 0,

    pub fn Pack(rcv: FixedSizeListT, __builder: *Builder, __pack_opts: fb.common.PackOptions) fb.common.PackError!u32 {
        _ = .{__pack_opts};
        var __tmp_offsets = std.ArrayListUnmanaged(u32){};
        defer if (__pack_opts.allocator) |alloc| __tmp_offsets.deinit(alloc);
        try FixedSizeList.Start(__builder);
        try FixedSizeList.AddListSize(__builder, rcv.listSize);
        return FixedSizeList.End(__builder);
    }

    pub fn UnpackTo(rcv: FixedSizeList, t: *FixedSizeListT, __pack_opts: fb.common.PackOptions) !void {
        _ = .{__pack_opts};
        t.listSize = rcv.ListSize();
    }

    pub fn Unpack(rcv: FixedSizeList, __pack_opts: fb.common.PackOptions) fb.common.PackError!FixedSizeListT {
        var t = FixedSizeListT{};
        try FixedSizeListT.UnpackTo(rcv, &t, __pack_opts);
        return t;
    }

    pub fn deinit(self: *FixedSizeListT, allocator: std.mem.Allocator) void {
        _ = .{ self, allocator };
    }
};

pub const FixedSizeList = struct {
    _tab: fb.Table,

    pub fn GetRootAs(buf: []u8, offset: u32) FixedSizeList {
        const n = fb.encode.read(u32, buf[offset..]);
        return FixedSizeList.init(buf, n + offset);
    }

    pub fn GetSizePrefixedRootAs(buf: []u8, offset: u32) FixedSizeList {
        const n = fb.encode.read(u32, buf[offset + fb.Builder.size_u32 ..]);
        return FixedSizeList.init(buf, n + offset + fb.Builder.size_u32);
    }

    pub fn init(bytes: []u8, pos: u32) FixedSizeList {
        return .{ ._tab = .{ .bytes = bytes, .pos = pos } };
    }

    pub fn Table(x: FixedSizeList) fb.Table {
        return x._tab;
    }

    /// Number of list items per value
    pub fn ListSize(rcv: FixedSizeList) i32 {
        const o = rcv._tab.offset(4);
        if (o != 0) {
            return rcv._tab.read(i32, o + rcv._tab.pos);
        }
        return 0;
    }

    pub fn MutateListSize(rcv: FixedSizeList, n: i32) bool {
        return rcv._tab.mutateSlot(i32, 4, n);
    }

    pub fn Start(__builder: *Builder) !void {
        try __builder.startObject(1);
    }
    pub fn AddListSize(__builder: *Builder, listSize: i32) !void {
        try __builder.prependSlot(i32, 0, listSize, 0);
    }

    pub fn End(__builder: *Builder) !u32 {
        return __builder.endObject();
    }

    pub fn Unpack(rcv: FixedSizeList, __pack_opts: fb.common.PackOptions) !FixedSizeListT {
        return FixedSizeListT.Unpack(rcv, __pack_opts);
    }
    pub fn FinishBuffer(__builder: *Builder, root: u32) !void {
        return __builder.Finish(root);
    }

    pub fn FinishSizePrefixedBuffer(__builder: *Builder, root: u32) !void {
        return __builder.FinishSizePrefixed(root);
    }
};
