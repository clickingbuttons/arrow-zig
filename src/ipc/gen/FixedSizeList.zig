//! generated by flatc-zig from Schema.fbs

const flatbuffers = @import("flatbuffers");

pub const FixedSizeList = struct {
    /// Number of list items per value
    list_size: i32 = 0,

    const Self = @This();

    pub fn init(packed_: PackedFixedSizeList) flatbuffers.Error!Self {
        return .{
            .list_size = try packed_.listSize(),
        };
    }

    pub fn pack(self: Self, builder: *flatbuffers.Builder) flatbuffers.Error!u32 {
        try builder.startTable();
        try builder.appendTableField(i32, self.list_size);
        return builder.endTable();
    }
};

pub const PackedFixedSizeList = struct {
    table: flatbuffers.Table,

    const Self = @This();

    pub fn init(size_prefixed_bytes: []u8) flatbuffers.Error!Self {
        return .{ .table = try flatbuffers.Table.init(size_prefixed_bytes) };
    }

    /// Number of list items per value
    pub fn listSize(self: Self) flatbuffers.Error!i32 {
        return self.table.readFieldWithDefault(i32, 0, 0);
    }
};
