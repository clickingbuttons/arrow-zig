//!
//! generated by flatc-zig
//! binary:     src/ipc/gen/format/Schema.bfbs
//! schema:     format/Schema.fbs
//! file ident: //Schema.fbs
//! typename    Struct_
//!

const std = @import("std");
const fb = @import("flatbufferz");
const Builder = fb.Builder;

/// A Struct_ in the flatbuffer metadata is the same as an Arrow Struct
/// (according to the physical memory layout). We used Struct_ here as
/// Struct is a reserved word in Flatbuffers
pub const Struct_T = struct {
    pub fn Pack(rcv: Struct_T, __builder: *Builder, __pack_opts: fb.common.PackOptions) fb.common.PackError!u32 {
        _ = .{__pack_opts};
        var __tmp_offsets = std.ArrayListUnmanaged(u32){};
        defer if (__pack_opts.allocator) |alloc| __tmp_offsets.deinit(alloc);
        _ = rcv;
        try Struct_.Start(__builder);
        return Struct_.End(__builder);
    }

    pub fn UnpackTo(rcv: Struct_, t: *Struct_T, __pack_opts: fb.common.PackOptions) !void {
        _ = .{__pack_opts};
        _ = rcv;
        _ = t;
    }

    pub fn Unpack(rcv: Struct_, __pack_opts: fb.common.PackOptions) fb.common.PackError!Struct_T {
        var t = Struct_T{};
        try Struct_T.UnpackTo(rcv, &t, __pack_opts);
        return t;
    }

    pub fn deinit(self: *Struct_T, allocator: std.mem.Allocator) void {
        _ = .{ self, allocator };
    }
};

pub const Struct_ = struct {
    _tab: fb.Table,

    pub fn GetRootAs(buf: []u8, offset: u32) Struct_ {
        const n = fb.encode.read(u32, buf[offset..]);
        return Struct_.init(buf, n + offset);
    }

    pub fn GetSizePrefixedRootAs(buf: []u8, offset: u32) Struct_ {
        const n = fb.encode.read(u32, buf[offset + fb.Builder.size_u32 ..]);
        return Struct_.init(buf, n + offset + fb.Builder.size_u32);
    }

    pub fn init(bytes: []u8, pos: u32) Struct_ {
        return .{ ._tab = .{ .bytes = bytes, .pos = pos } };
    }

    pub fn Table(x: Struct_) fb.Table {
        return x._tab;
    }

    pub fn Start(__builder: *Builder) !void {
        try __builder.startObject(0);
    }
    pub fn End(__builder: *Builder) !u32 {
        return __builder.endObject();
    }

    pub fn Unpack(rcv: Struct_, __pack_opts: fb.common.PackOptions) !Struct_T {
        return Struct_T.Unpack(rcv, __pack_opts);
    }
    pub fn FinishBuffer(__builder: *Builder, root: u32) !void {
        return __builder.Finish(root);
    }

    pub fn FinishSizePrefixedBuffer(__builder: *Builder, root: u32) !void {
        return __builder.FinishSizePrefixed(root);
    }
};
