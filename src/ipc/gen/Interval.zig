//! generated by flatc-zig from Schema.fbs

const flatbuffers = @import("flatbuffers");
const types = @import("lib.zig");

pub const Interval = struct {
    unit: types.IntervalUnit = .year_month,

    const Self = @This();

    pub fn init(packed_: PackedInterval) flatbuffers.Error!Self {
        return .{
            .unit = try packed_.unit(),
        };
    }

    pub fn pack(self: Self, builder: *flatbuffers.Builder) flatbuffers.Error!u32 {
        try builder.startTable();
        try builder.appendTableFieldWithDefault(types.IntervalUnit, self.unit, .year_month);
        return builder.endTable();
    }
};

pub const PackedInterval = struct {
    table: flatbuffers.Table,

    const Self = @This();

    pub fn init(size_prefixed_bytes: []u8) flatbuffers.Error!Self {
        return .{ .table = try flatbuffers.Table.init(size_prefixed_bytes) };
    }

    pub fn unit(self: Self) flatbuffers.Error!types.IntervalUnit {
        return self.table.readFieldWithDefault(types.IntervalUnit, 0, .year_month);
    }
};
