Please contribute~! @thesmartwon on [Zig's Discord](https://discord.com/invite/zig)

This project is just a Zig library.

```sh
zig build test
```

If you touch the `ipc` folder, run:
```sh
zig build test-integration
```
